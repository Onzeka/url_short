# -----------------------------------------------------------------------------
# Stoik Link - Local Development Environment Variables
# -----------------------------------------------------------------------------

# Database Connection (Prisma)
# Default values align with the docker-compose.yml configuration
DATABASE_URL="postgresql://stoik_user:stoik_password@localhost:5432/stoik_db?schema=public"

# Backend server configuration
PORT=3000
